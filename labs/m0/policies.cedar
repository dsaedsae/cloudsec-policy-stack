// M0 작업 파일 — 여기에만 작성한다. (정답지 cedar/policies.cedar 는 졸업 전 열람 금지)
//
// 스펙: labs/m0/README.md Step 2 (R1–R4), Step 4 (E1)
// 스키마(열람 OK — 스펙의 일부): cedar/schema.json
// 채점:  .venv\Scripts\python.exe labs\m0\grade.py        (core 8)
//        .venv\Scripts\python.exe labs\m0\grade.py --ext  (졸업: core 8 + ext 3)
//
// 시작 상태에서도 5/8이 통과한다 — 그 이유를 설명할 수 없으면 README Step 0부터.

// R1: 계좌 조회는 그 계좌의 소유자만
// TODO

// R2: 이체는 소유자 + 양수 금액 + 본인 transferLimit 이하
// TODO

// R3: 감사로그 조회는 auditor 역할만
// TODO

// R4: 동결(frozen) 계좌의 이체는 무조건 금지 (어떤 허용보다 우선)
// TODO

// E1 (졸업 과제): 감사역은 모든 계좌 조회 가능 — 단, 이체는 절대 불가
// TODO
