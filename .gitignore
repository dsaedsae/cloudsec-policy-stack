# Python
.venv/
__pycache__/
*.pyc

# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfplan

# Kubeconfig / secrets (path-anchored — kind writes terraform/<cluster>-config)
kubeconfig
*.kubeconfig
**/*-config
*.pem
*.key
*-key.pem
terraform/cloudsec-config
